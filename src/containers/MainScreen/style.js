import { makeStyles } from '@material-ui/styles';

const style = makeStyles({
  mainScreen: {
    width: '100%',
    height: '100vh',
    background: 'teal',
    position: 'relative',
    textAlign: 'center',
  },
});

export default style;
